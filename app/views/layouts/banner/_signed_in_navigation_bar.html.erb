
<div class="collapse navbar-collapse topNavbar" id="signOutLinkOnTopNav">
  <% if user_signed_in? %>
    <ul class="nav navbar-nav navbar-right">
      <% unless params[:action] == 'home' %>
        <li><%= link_to t('topnav.signout'), destroy_user_session_path, method: :delete,
                        class: 'btn btn-primary', confirm: 'Are you sure?' %></li>
      <% end %>
    </ul>
  <% end %>
</div>

