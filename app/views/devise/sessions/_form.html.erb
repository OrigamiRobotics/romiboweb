<div id="sign_in_panel" class="container-fluid">
  <%= form_for(resource, :as => resource_name,
               :url => session_path(resource_name), role: 'form') do |f|
  %>
    <div class="form-group">
      <%= f.label :email, class: 'sr-only' %>
      <%= f.text_field :email, :required => false,
                       :autofocus => true,
                       class: 'form-control input-sm',
                       placeholder: t('home.placeholder.email') %>
    </div>

    <div class="form-group">
      <%= f.label :password, class: 'sr-only' %>
      <%= f.password_field :password, :required => false,
                           class: 'form-control input-sm',
                           placeholder: t('home.placeholder.password') %>
    </div>


    <div class="checkbox">
      <label>
        <%= f.check_box :remember_me, :as => :boolean if devise_mapping.rememberable? %>
        <%= t 'home.placeholder.remember_me' %>
      </label>
    </div>
		<div>
		  <%= f.submit t('home.button.signin'),
		               class: "btn btn-primary btn-xs btn-block",  id: "signUpButton" %>
		</div>


  <% end %>
</div>
