<%= link_to '<i class="glyphicon glyphicon-plus"></i> '.html_safe + t('palettes.edit.button').html_safe,
            buttons_path(palette_id: palette.id, js: true, status: "new"),
            method: :post,
            remote: true,
            id: 'addNewButton'
%>